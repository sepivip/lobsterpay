# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.next/
.turbo/
target/

# Environment
.env
.env.local
.env.*.local
.env.deploy
.env.integration

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Anchor
programs/lobsterpay/target/
test-ledger/
.anchor/

# Logs
*.log
npm-debug.log*

# Coverage
coverage/
.nyc_output/

# Misc
*.tsbuildinfo
.playwright-mcp/
*.png
*.jpg

# Web public/ assets are intentional site artifacts (OG card, brand SVGs,
# icon.svg). Whitelist back in over the broad *.png / *.jpg rules above.
!apps/web/public/*.png
!apps/web/public/*.jpg

# Marketing assets at the repo root (social preview, hero PNGs) are also
# intentional. Without these whitelist lines, the broad *.png / *.jpg
# rules silently drop them on `git add`.
!public/marketing/**/*.png
!public/marketing/**/*.jpg

# Claude Code session transcripts (may contain pasted secrets)
claude-session.jsonl
*.session.jsonl

# Installed third-party CLI skills (not part of project source).
# The leading slash on /skills/ anchors to repo root so apps/api/src/skills
# (project source) is NOT ignored.
.claude/skills/
.agents/skills/
/skills/
.continue/
.windsurf/

# Third-party reference source copied locally during integration research
# (e.g. agon-gateway and coinbase/x402 repo snapshots). Don't vendor these
# into our tree - they're other repos' code and must not be checked in.
scripts/agon-refs/
