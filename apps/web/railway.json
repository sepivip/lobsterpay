{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @lobsterpay/shared build && pnpm --filter @lobsterpay/web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @lobsterpay/web start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
